import { Box } from '@chakra-ui/react';
import React from 'react';
import Filter from '../components/filter/Filter';

const Home = () => {
  return (
    <Box mt="-4rem">
      <Filter />
    </Box>
  );
};

export default Home;
